<section class='no_body_content_left no_body_content_right' id='body_content'>
  <section id='body'>
    <div class='inner'>
      <%= raw @page.content_for(:body) %>
    </div>
    <%= yield :body_content_left %>
  </section>

  <section id='side_body'>
    <div class='inner'>
      <%= raw @page.content_for(:side_body) %>
    </div>
    <%= render :partial => "common/weather" %>
  </section>
</section>
